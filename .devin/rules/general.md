---
trigger: always_on
---

Code:

- Always write the code in English, as well as the comments, even when the prompt is not in English.
- Always use pnpm instead of npm.
- Never run git commit or git push. Only read-only git operations are allowed (e.g. git status, git log, git diff). The user manages all commits and pushes themselves.
